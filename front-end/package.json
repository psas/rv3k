{
  "name": "rv3k",
  "private": true,
  "version": "0.0.3",
  "description": "A telemetry viewer for PSAS",
  "repository": "https://github.com/patter5/rv3k",
  "license": "gpl v2",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "update-deps": "npm update",
    "postupdate-deps": "bower update",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
